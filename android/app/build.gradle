plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.sun_direction" // Your app's namespace
     compileSdk = 34 // Update to the latest stable version

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        applicationId = "com.example.sun_direction"
        minSdk = 21 // Set to the minimum supported SDK version
        targetSdk = 34 // Match your compileSdk version
        versionCode = 1 // Increment this for releases
        versionName = "1.0" // Set the version name
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}
